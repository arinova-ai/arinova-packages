---
"@arinova-ai/cli": patch
---

Unify resource CRUD, pagination, export, permissions, and unavailable-command registration. List requests now send a bounded default limit, automation products are split into focused modules, and command-scoped clients are reused across related requests.
