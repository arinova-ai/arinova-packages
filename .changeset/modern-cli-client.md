---
"@arinova-ai/cli": minor
---

Route all CLI commands through the configured `ApiClient`, add conditional ETag reads, and warn when credentials are supplied through the process-visible `--token` flag. Remove the retired `wiki` alias and unsupported Kanban card archive, card-label list, slide member, and lounge command surfaces.
