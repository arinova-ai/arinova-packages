---
"@arinova-ai/agent-sdk": patch
---

Split WebSocket connection binding, authentication retry, outbound buffering, and fair task scheduling into focused modules while preserving the `ArinovaAgent` API. Export the runtime and aggregate board result type contracts from the package entry point.
