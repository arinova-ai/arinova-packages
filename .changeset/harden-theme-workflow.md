---
"@arinova-ai/cli": patch
---

Validate theme manifests consistently before upload, update, development, and build; split theme commands into focused modules; and make the embedded theme bridge dependency and drift checks explicit. Remote list commands now send a bounded default page size through the shared pagination helper.
