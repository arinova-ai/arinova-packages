---
"@arinova-ai/cli": patch
---

Send sticker images and sticker-pack covers using the multipart `file` field required by the server, and expose managed Painter cover uploads so albums can satisfy publication readiness from the CLI.
