---
"@arinova-ai/mcp-server": minor
---

Harden manifest and action HTTP boundaries, validate generated MCP tools and arguments, expose structured errors and intermediate results, make shutdown cancellable, and add real MCP transport plus CLI and coverage regression tests. Requires Node >= 20.10 (JSON import attributes).
