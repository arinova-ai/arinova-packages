---
"@arinova-ai/cli": patch
---

Fix managed Space list and version commands, remove retired cron confirmation endpoints, allow local Space build commands in non-interactive use, and refresh the server route/auth contracts.
