{
  "name": "@arinova-ai/cli",
  "version": "0.2.0",
  "description": "Arinova CLI — manage messages, notes, kanban, memory, creator tools, and more",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arinova-ai/arinova-packages.git",
    "directory": "packages/arinova-cli"
  },
  "type": "module",
  "bin": {
    "arinova": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "node scripts/inline-bridge.mjs",
    "build": "tsc",
    "bridge:check": "node scripts/inline-bridge.mjs --check",
    "contracts:generate": "node scripts/generate-v1-routes.mjs",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "pretest": "node scripts/inline-bridge.mjs --check && tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@arinova-ai/theme-sdk": "workspace:*",
    "@types/node": "^22.19.13",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
